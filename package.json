{
  "name": "session-deck",
  "version": "0.1.0",
  "description": "Web-based tmux workspace manager with live terminal panes",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev:server": "node --watch src/index.js",
    "dev:client": "cd frontend && npm run dev",
    "dev": "npm run dev:server & npm run dev:client",
    "build": "cd frontend && npm run build",
    "test": "node --test test/*.test.js",
    "test:integration": "node --test integration-tests/*.test.js",
    "audit:prod": "npm audit --omit=dev && npm --prefix frontend audit --omit=dev",
    "verify": "npm test && npm run build && npm run audit:prod",
    "postinstall": "cd frontend && npm install"
  },
  "keywords": [
    "tmux",
    "terminal",
    "workspace-manager"
  ],
  "author": "Jesse Jones",
  "license": "MIT",
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/formbody": "^8.0.2",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/session": "^11.1.1",
    "@fastify/static": "^10.1.2",
    "@fastify/websocket": "^11.2.0",
    "better-sqlite3": "^12.8.0",
    "fastify": "^5.8.4",
    "node-pty": "^1.1.0"
  }
}
